namespace MyShatteredPixelDungeon.scripts.menu.inventory_page;

public partial class InventoryPage
{
    /// <summary>
    ///     连接 Godot 信号
    /// </summary>
    private void ConnectPageSignals()
    {
        // %CloseButton.Pressed += () => CloseInventory();
        // %ItemList.ItemSelected += (index) => SelectItem(index);
    }
}